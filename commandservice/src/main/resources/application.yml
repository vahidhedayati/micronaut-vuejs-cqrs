micronaut:
  application:
    name: commandapp
  server:
    port: ${random.port}
consul:
  client:
    registration:
      enabled: true
    defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
---
kafka:
  #  embedded:
  #    enabled: true
  bootstrap:
    servers: "${KAFKA_HOST:localhost}:${KAFKA_PORT:9092}"